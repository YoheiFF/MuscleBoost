---
project_id: "2026-09-14-1727-remove-seconds-per-set"
phase: pm-report
overall_status: completed
created: "2026-09-14"
---

# 最終報告 - 2026-09-14-1727-remove-seconds-per-set

## 依頼
記録追加フォームの「1セットあたり秒数（任意）」入力欄と「セット数×秒数から時間を計算」ボタンを削除する。

## 情報収集（Phase 1）
- 対象UIはDBには一切保存されない、`WorkoutLogForm.tsx`内のローカルstateのみで完結する補助機能と確認。
- 削除対象4箇所（import・state・handler・JSX）を特定。「運動時間（分）」欄は別実装で影響を受けないことを確認。

## 設計（Phase 2）
- UI削除に伴い、唯一の呼び出し元だった `src/lib/calorie.ts` の `estimateDurationMinutes()` と関連テストもデッドコードとして合わせて削除する方針を確定（カロリー計算本体には影響なし）。

## 実装（Phase 3）
- `WorkoutLogForm.tsx`・`calorie.ts`・`calorie.test.ts` の3ファイルを設計書通りに編集。
- `npx tsc --noEmit` 0エラー、`npm run build` 成功、`npm run test` 25件全パス。

## テスト（Phase 4）
- 総合判定: **pass**
- 静的検証・単体テスト全件成功に加え、実際にブラウザで記録作成フォームを開き、対象UIが完全に消えていること、記録の保存自体は正常に動作すること（159.8kcalで保存成功）を確認済み。
- test-report: `.project/qa/2026-09-14-1727-remove-seconds-per-set/test-report.md`

## QA中に発生したインシデントと対応
1. **DB接続先の誤検知**: QAがプロセスツリーの調査を途中で打ち切り、「本番Turso DBに接続している疑いがある」と一時誤報告。PMがプロセスツリーを完全に遡って追跡し、`npm run dev:local`（ローカルSQLite固定スクリプト）経由の正しい起動であることを確認し誤報と判明。実害なし。
2. **devサーバーのビルドキャッシュ破損**: QAが稼働中のdevサーバーと共有する`.next`ディレクトリに対し`npm run build`を実行し、キャッシュが破損（画面のCSS/JSが読み込めなくなる503障害）。QAが部分的な復旧を試みた後、権限上の制約で操作不能になったため、PMが以下で復旧:
   - プロセスツリーを完全停止
   - `.next`を丸ごと削除
   - `npm run dev:local`で再起動、正常動作を実機確認
- いずれも**本番環境・本番データへの影響はなし**（ローカル開発環境内で完結する問題）。

## 残課題
なし（本プロジェクトのスコープでは完了）。前プロジェクトからの既存申し送り（ESLint未設定、本番マイグレーション未適用の累積分）は別途対応が必要。

## 次のアクション提案
1. 開発中はdevサーバー稼働中の`npm run build`実行を避けるか、実行後は`.next`削除＋サーバー再起動をセットで行う運用を徹底する
2. ここまでの4プロジェクト分（マシン統一・重さ入力／トレーニングボリューム／プロフィール体重身長一元化／秒数欄削除）をまとめてコミットするか確認
