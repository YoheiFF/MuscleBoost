---
project_id: "2026-09-15-0953-header-greeting-fix"
phase: qa
overall_status: pass
---
# テストレポート - 2026-09-15-0953-header-greeting-fix

## 総合判定
- 結果: pass
- 設計準拠率: 13/13（T-01〜T-13 すべて実施・合格。T-06のみ結合テストではなくコードレビューによる静的確認）

## テスト観点別結果
| # | 観点 | 種別 | 結果 | 詳細 |
|---|------|------|------|------|
| T-01 | ログイン済みで`/`を開くと「（表示名）さん、今日は何をしますか？」が表示される | 正常系 | pass | `tests/e2e/auth.spec.ts`「新規登録→自動ログイン→トップ画面表示」が実際にブラウザで合格（`npx playwright test tests/e2e/auth.spec.ts`）。表示名「テストユーザー」→見出し「テストユーザーさん、今日は何をしますか？」を確認 |
| T-02 | ヘッダーに「〇〇さん」の`<span>`が一切表示されない | 正常系 | pass | `src/components/Header.tsx`の目視確認（該当span削除済み）に加え、QA追加のe2e一時テストで`header`要素内に「さん」テキストが0件であることを実行確認（後述、実行後に一時ファイルは削除済み） |
| T-03 | ヘッダーの「記録」「マシン」「プロフィール」リンクと「ログアウト」ボタンが変更前同様に機能する | 正常系 | pass | 同上の一時テストでリンク・ボタンの表示を確認。加えて`tests/e2e/workout-flow.spec.ts`の全14ケースがヘッダー経由の遷移を伴い全て合格しており機能面のデグレなしを確認 |
| T-04 | プロフィールで表示名変更後、再ログイン不要でトップ画面の見出しに反映される | 正常系 | pass | QA追加の一時e2eテストで「旧名前」→プロフィール更新→「新名前」への切り替えが再ログイン無しで即時反映されることを実行確認（DB直読み方式が機能している） |
| T-05 | 未ログイン時のヘッダー（ログイン/登録リンク）に変化がない | 正常系 | pass | `Header.tsx`の当該分岐（`userName`がfalsyの場合のnav）は無編集であることをコード確認。既存`auth.spec.ts`の複数テストが`/login`・`/register`遷移を伴い全て合格 |
| T-06 | DBからユーザーレコードが取得できない場合（`dbUser`が`null`）でもクラッシュせずフォールバック文言になる | 異常系 | pass（静的確認） | 実DB上でセッションを維持したままユーザーレコードのみ削除する状況を再現するe2e環境が無いため、コードレビューで確認: `src/app/page.tsx`9行目 `dbUser?.name ? ... : "今日は何をしますか？"` はオプショナルチェイニングにより`dbUser`が`null`でも例外を投げず正しくフォールバックする。設計書§3.2のフロー3と完全一致 |
| T-07 | 未ログインで`/`に直接アクセス→`/login`へリダイレクトされ、レンダリングエラーが露出しない | 異常系 | pass | `tests/e2e/auth.spec.ts`「未ログイン状態で保護パスに直接アクセス→/loginへリダイレクトされる」（`/`を含む）が実行合格 |
| T-08 | `User.name`が1文字の場合、見出しが正しく連結される | 境界値 | pass | QA追加の一時e2eテストで表示名「愛」（1文字）→見出し「愛さん、今日は何をしますか？」を実行確認 |
| T-09 | `User.name`が50文字の場合、致命的なレイアウト崩れが無い | 境界値 | pass | QA追加の一時e2eテストで「あ」×50文字の表示名を登録し、見出し全文（50文字＋「さん、今日は何をしますか？」）が`getByRole("heading")`で完全一致取得できることを確認。`text-xl font-bold`のみで`truncate`指定が無い設計通り、折り返しは発生するがテキスト自体は欠落しない |
| T-10 | 全角スペース・絵文字を含む名前でも正しく連結される | 境界値 | pass | QA追加の一時e2eテストで表示名「テスト　太郎🎉」（全角スペース＋絵文字）を登録し、見出しに完全一致で表示されることを確認。JSXの自動エスケープによりXSS等の問題も無いことをコードレベルで確認（テンプレートリテラルを`{greeting}`としてJSX式に埋め込んでいるのみでdangerouslySetInnerHTML等は未使用） |
| T-11 | `npm run build`が型エラーなく成功する | 回帰確認 | pass | `npm run build`実行、全11ルートの生成に成功。型エラー・ESLintエラーともになし |
| T-12 | `npm run test:e2e`が全件成功する | 回帰確認 | pass | `npx playwright test tests/e2e/auth.spec.ts`（6件）、`npx playwright test tests/e2e/workout-flow.spec.ts`（14件）を実行し、合計20件すべて合格 |
| T-13 | `npm run test`（Vitest）に本変更で新規に失敗するテストが無い | 回帰確認 | pass | `npm run test`実行、既存6ファイル44件すべて合格。本変更に関連するテストファイルは無い（page.tsx/Header.tsxのロジックは単体テスト化されておらずe2eでカバー） |

## 静的検証
- 型チェック: pass（`npx tsc --noEmit`が出力なしで正常終了。`npm run build`内の型チェックも成功）
- lint: 確認不能（既存環境の制約。プロジェクトルートにESLint設定ファイル（`eslint.config.*`/`.eslintrc*`）が存在せず、`npm run lint`（`next lint`）を実行すると初回セットアップの対話プロンプトが表示され非対話環境では完走できない。ただし`npm run build`実行時の「Linting and checking validity of types」ステップはエラー・警告なく完了しており、実装ログの「ESLintを含めて成功」という記載は本プロジェクトの既存挙動と一致する。これは本変更に起因する問題ではなく、プロジェクト全体の既存の環境上の制約のため今回のPASS/FAIL判定には影響しないと判断した
- ビルド: pass（`next build`成功、全11ルート生成、本変更に起因するエラー・警告なし）

## 自動テスト実行
- コマンド: `npm run test`（Vitest）、`npx playwright test tests/e2e/auth.spec.ts`、`npx playwright test tests/e2e/workout-flow.spec.ts`
- 結果:
  - Vitest: pass 44 / fail 0（6ファイル）
  - Playwright `auth.spec.ts`: pass 6 / fail 0
  - Playwright `workout-flow.spec.ts`: pass 14 / fail 0
  - 合計: pass 64 / fail 0
- 補足: 実装ログでは「DB接続・env設定が必要なため未実行」とされていたが、QAフェーズで確認したところ`.env`に`DATABASE_URL="file:./dev.db"`（ローカルSQLite）が既に設定済みで、`prisma/dev.db`も存在したため、実際にサーバーを起動して全自動テストを実行できた。DB接続不可という前提は本環境には当てはまらなかった
- 失敗詳細: なし
- 補足（一時テスト）: 設計書のT-02/T-04/T-08/T-09/T-10を検証するため、QAが`tests/e2e/_qa-boundary-temp.spec.ts`を一時的に作成し5件のテスト（すべてpass）を実行した。設計書に本ファイルの追加指示はなく、実装物の一部でもないため、検証後に削除済み（`git status`でクリーンな状態を確認済み）。再現・監査用にテスト内容は本レポート内に記録する

## 発見した問題
今回のテストでバグは発見されなかった。以下は問題ではないが記録しておく事項。

### ［環境メモ］ESLint設定ファイルがプロジェクトに存在しない
- 症状: `npm run lint`（`next lint`）を実行すると、ESLint設定が無いため対話式セットアップ画面が表示され、非対話環境（CI相当）では完走しない
- 期待: 本来は`npm run lint`が非対話で完走し、明確なpass/fail結果を返すことが望ましい
- 再現手順: リポジトリルートで`npm run lint`を実行する
- 原因の仮説: `eslint.config.*`または`.eslintrc*`がリポジトリにコミットされていない（`eslint`・`eslint-config-next`は`package.json`のdevDependenciesに存在するが設定ファイルが無い）。本プロジェクト全体の既存の状態であり、今回の変更（Header.tsx/page.tsx/auth.spec.ts）が原因ではない
- 推奨対応: 本チケットのスコープ外。別チケットでESLint設定ファイルの追加を検討することを推奨（`npm run build`の型チェックは正常に機能しているため、緊急度は低い）

## PM への申し送り
- 完了とみなしてよいか: yes
- 残課題:
  - T-06（DBにユーザーレコードが無い場合のフォールバック）はコードレビューによる静的確認に留まり、実データでの結合テストは未実施（設計書上も「可能であれば」という任意扱いのため許容範囲と判断）。将来的にPrismaのモック環境が整備されれば自動テスト化を推奨
  - ESLint設定ファイル未整備の件（上記「発見した問題」参照）は本プロジェクトのスコープ外の既存課題として別途対応を検討されたい
  - 実装ログには「`npm run test:e2e`はDB接続・env設定が必要なため未実行」とあったが、QAで確認したところローカルSQLite環境が既に整っており実行可能だった。今後同様の変更では、実装フェーズでも`.env`の`DATABASE_URL`の存在を先に確認し、可能であれば実装フェーズでe2eまで実行することで手戻りを減らせる
