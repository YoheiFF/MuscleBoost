---
project_id: "2026-09-16-0032-heatmap-6months"
created: "2026-09-16"
overall_status: completed
---

# 最終報告: 2026-09-16-0032-heatmap-6months

## 依頼
カレンダーヒートマップの表示期間を「当月＋過去2ヶ月＝3ヶ月分」から「当月＋過去5ヶ月＝合計6ヶ月分」に変更する。

## 対応方針
直前のプロジェクト（2026-09-15-2347-heatmap-monday-3months）で実装したばかりの
`computeHeatmapWindowStartUtc`（`src/lib/achievements.ts`）が既に月数をパラメータ化した
設計になっており、`subtractJstMonths(now, 2)` → `subtractJstMonths(now, 5)` という
1パラメータの変更で対応可能と判断。設計の枠組み自体（月曜整列・暦月ベースの算出方式）に
変更は無いため、フルパイプライン（情報収集/設計/実装/QAの4エージェント起動）は行わず、
PMが直接・同等の厳密さ（実装コードを`npx tsx`で直接実行した独立検算、テスト全件実行、
ビルド、e2e回帰確認、ブラウザ実機確認）で対応した軽量フォローアップとして実施。

## 実施内容
- `src/lib/achievements.ts`: `subtractJstMonths(now, 2)` → `subtractJstMonths(now, 5)` に変更、関連コメント3箇所を「2ヶ月」→「5ヶ月」に更新
- `src/components/WorkoutHeatmap.tsx`: フッター文言「直近3ヶ月間」→「直近6ヶ月間」に更新
- `tests/unit/achievements.test.ts`: 3テストケースの期待値を再計算して更新
  - NOW=2026-09-15(火) → start=2026-03-30, windowDays=170
  - marNow=2026-05-04(月) → start=2025-12-01, windowDays=155
  - janNow=2026-01-15(木) → start=2025-07-28, windowDays=172
  - いずれも`npx tsx`で実装コード（`computeHeatmapWindowStartUtc`/`computeHeatmapWindowDays`）を直接実行し、手計算と実装の両方が一致することを確認した上でテストに反映（過去のQAで発覚した「設計書の期待値計算ミス」の再発防止のため）

## 検証結果
- 型チェック・ビルド: pass
- Vitest単体テスト: 85件全pass（achievements.test.ts 24件含む）
- Playwright e2e回帰: workout-flow.spec.ts 16件・auth.spec.ts 6件、計22件。1回目の実行でそれぞれ1件ずつ別のテストが失敗したが、いずれも登録直後のリダイレクトタイミングに起因する既知のフレーキーテスト（直前プロジェクトのQAレポートで報告済み、本変更の対象ファイルとは無関係）であることを個別再実行で確認し、再実行では全件pass
- ブラウザ実機確認（ローカルSQLite、dev:local）: ログイン後の実績画面で、曜日ラベル（月〜日）の整列、フッター文言「直近6ヶ月間の記録日数」、約26週分のグリッド表示を目視確認

## 残課題
- git working treeはクリーン。未コミット。
- 直前プロジェクト（2026-09-15-2347-heatmap-monday-3months）の設計書・QAレポートは3ヶ月版の記録のまま保持（本ファイルが最新状態の記録）

## 次のアクション提案
- 内容を確認の上、コミット・GitHubへのプッシュ（本番デプロイ）を実施することを推奨
